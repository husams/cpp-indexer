{
  "name": "cidx-graphview-prototype",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "generate": "python3 ../scripts/generate_result_protocol.py --check && node scripts/generate-catalog.mjs",
    "typecheck": "npm run generate && tsc --noEmit",
    "test": "npm run generate && node --experimental-strip-types --test test/*.test.ts",
    "build": "npm run generate && vite build",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "dependencies": {
    "cytoscape": "3.33.1"
  },
  "devDependencies": {
    "@types/node": "24.0.12",
    "ajv": "8.18.0",
    "typescript": "5.8.3",
    "vite": "7.3.6"
  }
}

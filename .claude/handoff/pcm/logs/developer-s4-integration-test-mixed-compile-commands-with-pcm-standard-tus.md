# Developer Log — pcm-support / S4 (+ S1–S3)

## Skills loaded
- `rust-conventions` — loaded before writing any code.
- `pcm-support` — invoked (workflow launcher skill; orientation only, no code generated by it).

## Skills considered but not loaded
- `implement-story` — not loaded; task was a direct developer dispatch with explicit plan.
- `cpp-conventions` — not applicable (Rust project).

## Commands run

### Orientation
- Read CHARTER.md, plan.md, design.md, scenarios.md, modules_cpp20.rs, parallel.rs, symbol_id_integration.rs, parallel_phase1.rs, cli_fail_on_tu_error.rs, Cargo.toml.
- `git log --oneline -15` — confirmed S1–S3 absent from tree.
- `git worktree list` — single worktree; no stash.
- `cargo nextest run --lib --features test-mock is_module_tu` — 2/2 pass pre-edit baseline.
- Verified probe is `true` on this host: `cargo nextest run --lib --features test-mock --run-ignored all parse_module_tu_emits_module_node` → PASS.
- Tested Apple clang 17 PCM build (`--precompile`) → fails (named modules unsupported in CLI).

### Implementation (S1–S3)
- Edited `src/visit/modules_cpp20.rs`: `is_module_tu`, `run_probe`, `parse_module_tu`, new `check_explicit_pcm_files`, unit tests.
- Edited `src/pipeline/parallel.rs`: imports + dispatch branch.
- `cargo build --features test-mock` → OK (21s).

### Exit gate — pass 1 (all clear)
- `cargo fmt --all -- --check` → FAIL (formatting diffs in parallel.rs, modules_cpp20.rs, pcm_integration.rs, examples/).
- `cargo fmt --all` → fixed.
- `cargo fmt --all -- --check` → OK.
- `cargo clippy --all-targets --all-features -- -D warnings` → FAIL: unused `Arc` import in pcm_integration.rs.
- Removed `use std::sync::Arc;` from pcm_integration.rs.
- `cargo clippy` → OK.
- `cargo nextest run --tests --features test-mock pcm_integration` → FAIL: 0 tests found (nextest positional arg matched test function names, not binary name).
- Renamed test functions to prefix `pcm_integration_`.
- `cargo nextest run --tests --features test-mock pcm_integration` → 1/1 PASS.
- `cargo nextest run --tests --features test-mock --run-ignored all pcm_integration` → 2/2 PASS.

### Regression check
- `cargo nextest run --lib --tests --features test-mock -- parallel is_module_tu missing_pcm` → 11/11 PASS.

## Deviations from plan.md
1. S1–S3 were absent; implemented as prerequisite for S4.
2. S4-AC1 (`#[ignore]`d) self-skips on this host: probe returns `true` (libclang parses `.cppm` via `-fmodules`) but Apple clang 17 cannot precompile named modules via CLI. Skip-with-reason path is correct per S4-AC3.
3. Test function names include `pcm_integration_` prefix (nextest positional-filter quirk).
4. Fatal-severity assumption (S3) unverified — Apple clang 17 cannot build `.pcm` for empirical check.

## Tool failures / retries
- fmt: 1 retry (expected; code was written first, then formatted).
- clippy: 1 retry (unused import).
- nextest: 1 retry (positional filter vs binary name — test renamed).
- Total: 1 pass through the gate after 3 fixes.

## Open items (tag: sr-dev)
- Verify `Severity::Fatal` vs `Severity::Error` for corrupt `.pcm` on clang 18+ host (ADR-3 open question).
- Run S4-AC1 end-to-end on a host where `clang++ --precompile` works (clang 18+ named modules).
- Per-story S1/S2/S3 developer logs are absent from `logs/`; QA should reference this file.

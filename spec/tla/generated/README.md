# Generated TLA+ output

This directory is reserved for disposable reports, traces, and translated
modules produced by future tooling. It is ignored by version control except
for this policy file. Generators may write only below this directory.

Protected invariants and trusted assumptions are hand-authored inputs. A
generator must never modify, replace, or delete files below `../protected/` or
`../trusted/`.
